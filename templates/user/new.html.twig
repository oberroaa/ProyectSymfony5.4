{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <section>
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    <h2>User new <span class="label label-default">{{ user.id }}</span></h2>
                </div>
            </div>
            <br>
            <div class="row">
                <div class="col-md-12">
                    {{ include('user/_form.html.twig') }}
                </div>

            </div>
        </div>
    </section>

{% endblock %}
